# Progress Log

<!-- Append-only. Never auto-loaded by agents. Read on-demand only.
     ONE line per task, no paragraphs:
     [YYYY-MM-DD] type(scope): what, why if not obvious (proof: commit:hash or file:path)
-->
