/// Endpoint constants. Fill in per-project.
///
/// Feature modules reference constants here as:
///   `ApiConstants.featureNameEndpoint`
abstract final class ApiConstants {
  /// Base URL for all API requests. Update per environment.
  static const String baseUrl = 'https://api.example.com/v1';

  // ── Add feature endpoints below ──────────────────────────────────────
  // static const String userProfileEndpoint = '$baseUrl/user/profile';
  // static const String settingsEndpoint = '$baseUrl/settings';
}
