# {{project_name.titleCase()}}

Generated by `mason make project` — hybrid state management architecture.

---

## Architecture Overview

```
lib/
├── l10n/             # Localization ARB files
├── networking/       # Dio engine, interceptors, sealed exceptions/responses
├── redux/            # Global state: authToken, userData, locale (ONLY these)
├── resources/        # Design tokens: colors, typography
├── services/         # Notification, device info stubs
├── screens/          # App screens & showcase
├── utils/            # AppScaffold, AppRouter, ShowMessage, CommonUtils, extensions
└── main.dart         # App entry point
```

## Adding Features

```bash
mason make bloc
```

This generates a complete feature folder with BLoC, Repo, Model, Page, Content widget, and Test.
